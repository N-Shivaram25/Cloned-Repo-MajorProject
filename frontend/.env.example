# Frontend environment example for Vite / Vercel
# Copy to .env.local for development (do NOT commit secrets)

# Stream API key used by the frontend
VITE_STREAM_API_KEY=your_stream_api_key_here

# Full backend base URL (include /api). When deploying frontend to Vercel,
# set this to your backend's production URL, for example:
# https://my-backend.vercel.app/api
VITE_BACKEND_URL=https://your-backend.vercel.app/api
